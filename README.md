How to use:

1. set an Environment Variable with the name DISCORD_TOKEN to the token to your Discord app.

2. uncomment the commented lines and run the Bot, this should register the four commands. (this process could take up to an hour)

3. stop the Bot again and delete the previously commented lines.

4. run the Bot again.
